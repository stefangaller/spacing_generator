name: example
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  spacing_generator_annotation:
    path: ../../spacing_generator_annotation

dev_dependencies:
  flutter_test:
    sdk: flutter
  spacing_generator:
    path: ../
  build_runner: any